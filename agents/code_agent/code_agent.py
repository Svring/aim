# from pydantic import BaseModel, Field
# from langchain_core.tools import tool

# from typing import Optional, Dict, Any
# from returns.future import future_safe


# @tool
# def invoke_codex(
#     project_address: str,
#     metadata: Optional[Dict[str, Any]] = None,
# ) -> str:
#     """Add a user's project to the codebase state."""
#     return "Hello, world!"


# @future_safe
# async def run_code_agent(project_address: str, prompt: str):
#     """Run the code agent."""
#     return "Hello, world!"
